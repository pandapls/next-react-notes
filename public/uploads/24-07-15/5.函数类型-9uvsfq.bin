###### 函数的写法

```js
// 若没设置默认值 或者用可选符合则是必传参数
const fn = function (name: string, age?: number) : string {
	return name + age
}
```

###### 接口写法

```js
interface User {
	name: string,
	age: number
}

const fn = function (user: User): User {
	 return user
}
```

###### 函数重载

```js
function fn(params: number):void
funciton fn(params: string, params2:number):viod
function fn(params:any, parmas2:any):any{
	console.log(params + params)
}

fn(1) // 输出第一套规则 1 + undefined
fn('1',2) // 遵循第二套规则 	'1' + 2

```


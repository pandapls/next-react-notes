###### 语法

```js
enum name {
	name1,
	name2,
}
```

默认枚举 从0开始

```
enum name {
	name1 = 0,
	name2 = 1,
}
```



增长枚举 从上个枚举值开始自增

字符串枚举，无法自增，需自行补全

```
const enum name{

}

enum name {

}
// 前者编译成常量
// 后者会编译成一个对象
```

###### 反向映射

字符串不支持反向映射

```
enum Types {
	success
}

let success:number = Types.success // 0
let key = Types[success] // success
```

